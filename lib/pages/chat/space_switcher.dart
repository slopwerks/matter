import 'package:flutter/widgets.dart';

@Deprecated('空间筛选已迁移到独立的空间页。')
class SpaceSwitcher extends StatelessWidget {
  const SpaceSwitcher({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
